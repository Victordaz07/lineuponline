# `src/types`

Tipos compartidos del dominio y de integración (Firestore, notas).

- `doctrine.ts` — lecciones, módulos, escrituras (Seeker Gospel v2).
- `userNotes.ts`, `topics.ts`, `userProgress.ts`, `users.ts` — payloads y entidades de backend.
