# `src/hooks`

Hooks de React reutilizables.

- `useLesson`, `useModule` — carga de contenido doctrinal.
- `useUserProgress` — progreso local (preparado para unificar con auth).
- `useSyncNotes` — guardado y sincronización de notas con Firestore.
